.DS_Store
node_modules/
dist/
*.log
.env
.env.local
.vscode/
.idea/
.claude/

# WAV masters live locally only — site ships MP3
content/*.wav


Content 2/

# Cloudflare analytics token lives at ~/.cloudflare-analytics-token
# (outside the repo) — gitignoring for safety in case it ever gets copied in
.cloudflare-analytics-token
